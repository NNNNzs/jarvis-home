# JARVIS System Configuration
# 复制此文件为 .env 并填充你的配置

# ==================== 必需配置 ====================

# OpenAI API Key (必需，用于 LLM)
# 获取: https://platform.openai.com/api-keys
OPENAI_API_KEY=sk-your-api-key-here

# ==================== 可选配置 ====================

# Home Assistant 配置 (如需真实设备控制)
# 获取 Token: HA用户配置 → 安全 → 长生命周期访问令牌
HOME_ASSISTANT_URL=http://homeassistant.local:8123
HOME_ASSISTANT_TOKEN=your_ha_long_lived_token_here

# LLM 模型配置
OPENAI_BASE_URL=https://api.openai.com/v1
LLM_MODEL=gpt-4o-mini
LLM_TEMPERATURE=0.3
LLM_MAX_RETRIES=3

# 缓存配置
# 策略: none | simple | context_aware (推荐)
CACHE_STRATEGY=context_aware
CACHE_TTL=3600          # 秒
CACHE_MAX_SIZE=50

# 服务器配置
PORT=3000
NODE_ENV=development
ALLOWED_ORIGINS=*

# 日志配置
LOG_LEVEL=info
VERBOSE_LOGS=true

# ==================== 高级配置 ====================

# Redis (可选，用于持久化缓存)
# REDIS_URL=redis://localhost:6379

# 小爱音箱 Webhook (米家场景触发)
# MI_SCENE_TRIGGER=https://your-webhook-url

# 最大并发
MAX_CONCURRENCY=5

# HA API 超时 (毫秒)
HA_TIMEOUT=10000
